git pull heroku master
git add .
git commit -m "tadd function search by school in pertinence"
git push heroku master

//
var query = {};
    query[fieldId] = id;
    var promise = Model.findOne(query);